# promoc_core

Die gepflegte Dokumentation fuer den Messstand liegt im Root-README:
[`../README.md`](../README.md).

In diesem Branch umfasst `promoc_core` nur noch `logging`,
`promoc_exceptions`, `error_handling` und `validation`.
