[flake8]
# Keep linting strict for package code, but ignore vendored/driver code we don't own.
exclude =
    build,
    install,
    log,
    .git,
    __pycache__,
    planar_motor_nodes/drivers,

# ament_flake8 enables some extra plugins in ROS environments.
# This config is intentionally minimal to avoid churn.
