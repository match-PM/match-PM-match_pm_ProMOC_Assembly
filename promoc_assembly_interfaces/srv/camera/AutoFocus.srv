# Request: Autofocus with Four-Step runtime defaults

# Start position for the scan (in mm)
float64 start_position

# End position for the scan (in mm)
float64 end_position

# --- COMPATIBILITY FIELD ---
# The CS runtime branch keeps one maintained autofocus path: Four-Step.
# The field remains in the interface for transition compatibility.
# Use 0 for the standard path; other values are accepted but mapped to Four-Step.
int32 focus_mode

# --- OPTIONAL: Skip fly-over detection (use full range) ---
# If true, skips fly-over and uses start_position to end_position directly
# Useful for deterministic algorithm comparisons on the same scan range
bool skip_flyover

# --- OPTIONAL: Save best image ---
# If true, saves best image to default autofocus result directory
bool save_best_image

# --- OPTIONAL MEASUREMENT CONDITIONS ---
# If objective_magnification_x is 0, objective is inferred from node parameter
float64 objective_magnification_x  # e.g. 1.0, 3.0, 6.0 (0 = auto/fallback)
bool use_beamsplitter              # True if beam splitter is in optical path
string notes

---

# Response: Result of the autofocus operation

bool success
string status_message
float64 best_focus_position
float64 best_focus_value
int32 total_measurements_taken
float64 duration_seconds
string best_image_path
float64[] measurement_positions
float64[] measurement_scores
